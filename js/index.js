let users = [{user:"us1",password:"us1"}]

function createUser(user,password){
    if(user === ""|| password ===""){
        return(alert("TODOS LOS CAMPOS SON OBLIGATORIOS"))
    }else{users.push({user,password})}
}























